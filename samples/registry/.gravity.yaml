emitters:
  csharp:
    output: gen/csharp
    namespace: AcmeCo.Domain
    file_scoped_namespaces: true
  # Phase 4 — JSON Schema (Draft-07) reference emitter. Uncomment to emit
  # per-entity bundles + per-value-type + per-enum schemas under gen/json-schema/.
  # Requires <PackageReference Include="Gravity.Dsl.Emitter.JsonSchema" />.
  # json-schema:
  #   output: gen/json-schema
  #   bundle_strategy: per-entity
