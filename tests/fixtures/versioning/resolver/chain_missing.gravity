namespace ops;

entity Employee version 1 {
    identity id: UUID;

    properties {
        name: String;
    }
}

entity Employee version 2 {
    identity id: UUID;

    properties {
        name: String;
    }
}
