namespace ops;

entity Employee version 1 {
    identity id: UUID;

    properties {
        old_name: String;
    }
}

entity Employee version 2 deprecates version 1 until "2099-01-01" {
    identity id: UUID;

    properties {
        new_name: String;
    }
}
