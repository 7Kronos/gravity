namespace ops;

entity Employee version 1 {
    identity id: UUID;

    properties {
        rank: Int;
    }
}

entity Employee version 2 deprecates version 1 until "2099-01-01" {
    identity id: UUID;

    properties {
        rank: Long;
    }
}
